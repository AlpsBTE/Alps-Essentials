name: Alps-Essentials
version: '1.5.1-SNAPSHOT'
main: com.alpsbte.essentials.AlpsEssentials
api-version: '1.21'

dependencies:
  server:
    LangLibs:
      required: true
      load: BEFORE
      join-classpath: true
    WorldGuard:
      required: true
      load: BEFORE
      join-classpath: true
    Alps-Hub:
      required: false
      load: AFTER
      join-classpath: true
    Alps-Terra:
      required: false
      load: AFTER
      join-classpath: true